#include<stdio.h>
int main()
{
int n,temp,sum=0,rem;
printf("enter number:");
scanf("%d",&n);
temp==n;
while(n>0)
{
rem=n%10;
sum+=(rem*rem*rem);
n/=10;
}
if(temp==sum)
printf("armstrong");
else
printf("not armstrong");
}
