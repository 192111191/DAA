#include<stdio.h>
int main()
{
int i,j,rows,k;
printf("enter rows:");
scanf("%d",&rows);
for(i=1;i<=rows;i++)
{
for(j=rows-i;j>=1;j--)
{
printf(" ");
}
for(k=1;k<=i;k++)
{
printf(" %d",k);
}
printf("\n");
}
}
